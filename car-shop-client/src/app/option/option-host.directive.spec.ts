import { OptionHostDirective } from './option-host.directive';

describe('OptionHostDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionHostDirective();
    expect(directive).toBeTruthy();
  });
});
